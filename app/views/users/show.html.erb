<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Docker cfg:</strong>
  <% if DockerCfg.find_by_id(@user.docker_cfg_id).nil? %>
    Error: there is no docker configuration
  <% else %>
    <%= link_to @user.docker_cfg_id, docker_cfg_path(DockerCfg.find_by_id(@user.docker_cfg_id)) %>
  <% end %>

</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
